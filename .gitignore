dist/
node_modules/
*.db
*.db-wal
*.db-shm
*.sqlite
# Benchmark scratch vaults. `*.db` does not catch these — the suffix is
# .db.work, and 7MB of them reached git before this line existed.
*.db.work*
bench/**/.cache/
eval-scale-data/
eval-*.ts
eval-*.py
eval-*.jsonl
eval-log.*
rescore-*.ts
generate_*.py
test/sim/eval-*
EVAL.md
EVAL-FRAMEWORK.md
agent-eval.md
hackernews-post.md
signal-quality-plan.md
.mcpregistry_*
.env*
!.env.example

# Agent working files — implementation plans are disposable once executed
.claude/
docs/superpowers/
.worktrees/

# Ablation scratchpad: a stack of ad-hoc score() probes rewritten per
# investigation, not a shared harness. It kept getting swept into unrelated
# commits. bench/locomo/run.ts and the dataset stay tracked.
bench/locomo/diag.ts
